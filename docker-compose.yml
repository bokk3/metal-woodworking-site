services:
  metalcraft-web:
    image: 3bok/metal-woodworking-site:latest
    ports:
      - "3003:3000"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
    # Uncomment to use environment file
    # env_file:
    #   - .env.local
